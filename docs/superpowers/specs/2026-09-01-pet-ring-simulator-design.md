# 宠环模拟器设计说明

本设计以 [PRD v0.1](../../../outputs/梦幻西游宠环模拟器-PRD-v0.1.md) 为唯一产品需求来源。

## 已确认决策

- 产品为部署在 `ring.ddctl.com` 的响应式网页。
- 不设计登录，个人周期、历史和物价只保存在当前浏览器。
- 首次访问明确询问是否参与匿名统计；拒绝不影响使用。
- 公共服务只收集任务及奖励概率需要的最小字段。
- 任务和奖励概率来自全站匿名样本；个人物价不上传。
- 后端使用 Go，公共样本使用独立 SQLite。
- 应用使用独立容器、数据卷和 Docker 网络。
- 仅复用现有 `supabase-caddy` 作为 HTTPS 入口，不使用 Supabase 其他组件。
- 第一版不连接或修改生产服务器，只交付可部署材料。
- 系统要求的任务与玩家处理方式是两个维度：任务用于统计出现概率，处理方式决定实际积分和成本。
- `mutant_specific` 支持按要求完成、上交非指定变异、上交非变异和跳过；其他任务支持按要求完成和跳过。
- 跳过本环固定扣 20 分、成本为零并进入下一环。
- 只有当前总积分至少 20 分时才允许跳过，界面选项与提交入口使用同一约束。
- 三药和烹饪提供“品质达标/品质不达标”处理方式；只有不达标时采集要求品质与实际品质。
- 公共样本不足时只展示确定性的方案积分和本地成本，不自动声称某个方案最优。
- 当前周期在宽度大于 900px 时采用左右双栏，窄屏保持单栏阅读顺序。

## 边界

第一版必须完成记环、积分计算、成本统计、平均值回退预测、公共模型预测、奖励档位、本地历史、本地物价、匿名选择、匿名样本 API、SQLite、限流、Docker 和 Caddy 配置示例。

第一版不做账号、跨设备同步、导入导出、排行榜、管理后台和公共物价。

## 可靠性原则

- 没有公共样本时使用当前周期平均积分和平均成本外推，并标记为低可信度。
- 公共样本不足时不得显示伪精确概率。
- 所有客户端输入在服务端重新校验。
- 单条匿名样本永不通过 API 返回。
- 前端网络失败不能阻断本地记账。
- 生产部署必须独立于现有 Supabase 数据和业务网络。

## 代码骨架

```text
cmd/pet-ring/          组装 HTTP 服务与嵌入式前端。
internal/domain/       定义任务、处理方式、积分、门槛与预测规则。
internal/httpapi/      校验匿名事件，并把请求转换为领域结果。
internal/store/        持久化任务要求、处理方式、实际积分与聚合模型。
web/model.mjs          浏览器端领域规则与确定性方案计算。
web/storage.mjs        本地周期数据及旧版变异记录迁移。
web/app.mjs            任务录入、处理方式选择和方案对比交互。
.github/workflows/     只负责通过受限 SSH 身份触发生产部署。
deploy/                保存管理员安装的部署脚本与基础设施配置样例。
```

依赖始终单向流动：界面依赖浏览器领域模型；HTTP 层依赖 Go 领域规则和存储接口；存储层不反向解释产品规则。数据库同时保存 `requested_score` 和实际 `score`，公共任务分布使用前者，避免玩家跳过或替代交付污染任务概率。GitHub Actions 只能触发服务器预装的 root 脚本，不能通过部署 Key 执行任意命令；生产脚本只拥有宠环仓库和容器的职责边界。

## 数据演进

- 本地旧记录 `mutant_unspecified` 自动迁移为 `requestedTaskType=mutant_specific`、`resolution=mutant_unspecified`。
- 本地旧记录 `normal_pet_as_mutant` 自动迁移为 `requestedTaskType=mutant_specific`、`resolution=normal_pet`。
- SQLite 启动时幂等增加 `resolution` 与 `requested_score`，并迁移旧变异记录。
- 成本仍只保存在浏览器；匿名事件上传任务要求、处理方式和规则积分，不上传价格。

## 变更日志

- 2026-09-01：分离任务要求与玩家处理方式，增加受积分约束的跳过、品质任务处理选项、指定变异四方案对比和响应式双栏。
- 2026-09-01：增加受限 SSH 触发的 GitHub Actions 自动部署、串行锁、健康检查与镜像回滚。
- 2026-09-01：桌面端将统计与起始数据置于右栏顶部，窄屏保持统计、录入、预测、明细的自然单栏顺序。
- 2026-09-01：部署触发时始终重建应用镜像，避免仓库已更新但容器仍运行旧静态资源。
- 2026-09-01：桌面双栏等高，主操作固定在左栏底部；窄屏继续按内容自然伸展。
- 2026-09-01：页头增加浅色、跟随系统、深色三段式主题选择，并在浏览器本地记忆偏好。
- 2026-09-01：任务选项增加语义图标并提高信息密度；本周期明细限制高度，超出后在卡片内部滚动。
- 2026-09-01：桌面录入操作固定在视口内；周期按钮前置；奖励只展示概率前三；历史删除改为明确的危险操作样式。
